// const { regexAbiertos } = require('../flows/allReportes/allAbiertosFlow')
// const { regexCerrados } = require('../flows/allReportes/allCerradosFlow')
// const { regexReportes } = require('../flows/allReportes/allReportesFlow')
// const { regexAyuda } = require('../flows/ayudaFlow')
// const { regexComentar } = require('../flows/comentar/comentarFlow')
// const { regexVer } = require('../flows/consultaReporte/estatoFlow')
// const { regexMenu } = require('../flows/inicioFlow')
// const { regexReportar } = require('../flows/reporteFlow')

// function coincideConRegexArray (palabra) {
//   const comandsRegex = [regexMenu, regexReportar, regexAyuda, regexVer, regexAbiertos, regexCerrados, regexComentar, regexReportes]

//   for (const regex of comandsRegex) {
//     if (regex.test(palabra)) {
//       return true // La palabra coincide con al menos una de las expresiones regulares
//     }
//   }
//   return false // La palabra no coincide con ninguna de las expresiones regulares
// }

// module.exports = {
//   coincideConRegexArray
// }
